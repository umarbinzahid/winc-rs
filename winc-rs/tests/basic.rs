#[test]
fn make_socket() {
    let mut f = [0u8; 10];
    let _writer = f.as_mut_slice();
    //let mut client = WincClient::<PrefixXfer<&mut [u8]>>::new();
    //let _res = client.socket();

    //let x = WincClient::from_xfer(writer, StubListener);
}
